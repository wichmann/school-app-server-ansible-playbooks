- name: Create users and set permissions
  ansible.buildin.import_tasks: user.yml

- name: Configure sshd and handle ssh keys
  ansible.buildin.import_tasks: ssh.yml

- name: Install necessary packages and update everything
  ansible.buildin.import_tasks: updates.yml

- name: Set a hostname
  become: true
  ansible.builtin.hostname:
    name: "{{ hostname }}"

- name: Set timezone
  become: true
  community.general.timezone:
    name: "{{ timezone }}"

- name: Ensure a locale exists
  become: true
  community.general.locale_gen:
    name: "{{ locale }}"
    state: present

- name: Create profile.d configuration
  become: true
  ansible.builtin.template:
    src: show_info.sh.j2
    dest: /etc/profile.d/show_info.sh
    mode: "0644"
